{
  "name": "db-v2-graphs",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ db-v2-graphs",
    "create-local": "graph create --node http://localhost:8020/ db-v2-graphs",
    "remove-local": "graph remove --node http://localhost:8020/ db-v2-graphs",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 db-v2-graphs",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.70.0",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
